//1
var array1 = {
  Array0: [2, 5, 10, "saya", 29, 99, 38],
  Array2: [100, 3, 66, 20, "abjad"],
  soal1: function() {
    var arrayLength1 = Array0.length;
    var arrayLength2 = Array2.length;
    if (arrayLength1 + arrayLength2 < 50) {
      for (var i = 0; i < arrayLength1; i++) {
        if (arrayLength1 % i == 0) {
          sum = arrayLength1 + arrayLength2;
          console.log(sum);
        } else {
          console.log(arrayLength1.arrayLength2);
        }
      }
    } else {
      console.log(false);
    }
    return this.soal1();
  },
};

//2
var array1 = {
  Array0: [3, 100, 283, "saya", "adalah", "kapiten", 75],
  Array2: [200, 300, 21, "dari", indonesia],
  soal1: function() {
    var arrayLength1 = Array0.length;
    var arrayLength2 = Array2.length;
    if (arrayLength2 + arrayLength2 != 32) {
      for (var i = 0; i < arrayLength1; i++) {
        if (arrayLength1 % i == 0) {
          sum = arrayLength1 + arrayLength2;
          console.log(sum);
        } else {
          console.log(arrayLength1.arrayLength2);
        }
      }
    } else {
      console.log(false);
    }
    return this.soal1;
  },
};

//3
a = 20;
var array1 = {
  Array0: [29, 12, 30, "ini", "saya", 85],
  Array2: ["cyber", "security", 20, 15, 21],
  soal1: function() {
    var arrayLength1 = Array0.length;
    var arrayLength2 = Array2.length;
    if (arrayLength2 + a == 28) {
      for (var i = 0; i < arrayLength1; i++) {
        if (arrayLength1 % i == 0) {
          sum = arrayLength1 + arrayLength2;
          console.log(sum);
        } else {
          console.log(arrayLength1.arrayLength2);
        }
      }
    } else {
      console.log(false);
    }
    return this.soal1;
  },
};

//4
a = 20;
var array1 = {
  Array0: [29, 12, 30, "ini", "saya", 85],
  Array2: ["cyber", "security", 20, 15, 21],
  soal1: function() {
    var arrayLength1 = Array0.length;
    var arrayLength2 = Array2.length;
    if (arrayLength2 + 1 != 35) {
      for (var i = 0; i < arrayLength1; i++) {
        if (arrayLength1 % i == 0) {
          sum = arrayLength1 + arrayLength2;
          console.log(sum);
        } else {
          console.log(arrayLength1.arrayLength2);
        }
      }
    } else {
      console.log(false);
    }
    return this.soal1;
  },
};

//5
a = 20;
var array1 = {
  Array0: [29, 12, 30, "ini", "saya", 85],
  Array2: ["cyber", "security", 20, 15, 21],
  soal1: function() {
    var arrayLength1 = Array0.length;
    var arrayLength2 = Array2.length;
    if (arrayLength2 != 35) {
      for (var i = 0; i < arrayLength1; i++) {
        if (arrayLength1 % i == 0) {
          sum = arrayLength1 + arrayLength2;
          console.log(sum);
        } else {
          console.log(arrayLength1.arrayLength2);
        }
      }
    } else {
      console.log(false);
    }
    return this.soal1;
  },
};

//6
u = 20;
var array1 = {
  Array0: [29, 12, 30, "ini", "saya", 85],
  Array2: ["cyber", "security", 20, 15, 21],
  soal1: function() {
    var arrayLength1 = Array0.length;
    var arrayLength2 = Array2.length;
    if (arrayLength2 + arrayLength1 + u != 25) {
      for (var i = 0; i < arrayLength1; i++) {
        if (arrayLength1 % i == 0) {
          sum = arrayLength1 + arrayLength2;
          console.log(sum);
        } else {
          console.log(arrayLength1.arrayLength2);
        }
      }
    } else {
      console.log(false);
    }
    return this.soal1;
  },
};
